#line 829 "../lpsrc/flx_regress.pak"
//Check fibres
#line 829 "../lpsrc/flx_regress.pak"
//Check channels

var y = ();
proc f() { print "Hello"; endl; }
f y;

var x = mk_schannel[unit]();

proc reader() {
  print "R1"; endl;
  var u = read x;
  print "R2"; endl;
}

proc writer() {
  print "W1"; endl;
  write (x,());
  print "W2"; endl;
}

spawn_fthread the reader;
spawn_fthread the writer;

print "Done"; endl;
