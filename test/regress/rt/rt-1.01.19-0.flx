#line 748 "../lpsrc/flx_regress.pak"
//Check recursion
#line 748 "../lpsrc/flx_regress.pak"
//Check tail call optimisation
fun g(x:int):int = {
  if x == 0 goto endoff;
  return g(x-1);
endoff:>
  return 1;
}

print (g 10); endl;
