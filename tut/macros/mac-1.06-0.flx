macro oldname is newname;
fun oldname(x:int):int = { return x; }
print (newname 1); endl;

macro proc make_ints ()
{
  macro vnew is new;
  val vnew = vold+1;
  macro vold = vnew;
}

val x = 1;

macro var vold = x;
make_ints;
make_ints;
make_ints;
print vold; endl;
